---
project: Mimir
phase: implementation
last_updated: 2026-04-28
tests:
  total: 704
  passing: 704
  failing: 0
blockers: []
ci_state: "2026-04-27 — Owner added more GitHub Actions usage and approved re-enabling Actions for this repo. Actions are currently enabled with allowed_actions=all. Keep the full local gate before push, batch related changes into one push, and avoid empty/speculative retry commits."
scope_change: "2026-04-24 — Mandate expansion: Mimir is now a multi-agent memory governance/control plane with governed consensus quorum and a transparent agent launch harness. Raw agent/project memories stay isolated by default; cross-project/operator/ecosystem reuse happens only through librarian-governed promotion with provenance, trust tier, scope, and revocation. Quorum outputs are governed evidence drafts, not truth or direct canonical writes. Users should eventually launch normal agents as `mimir <agent> [agent args...]`, preserving native terminal flows while Mimir handles bootstrap, rehydration, capture, and governance. See `docs/concepts/scope-model.md`, `docs/concepts/consensus-quorum.md`, `docs/planning/2026-04-24-multi-agent-memory-control-plane.md`, and `docs/planning/2026-04-24-transparent-agent-harness.md`. The 2026-04-21 prose-draft librarian pivot still stands."
next_milestone: "Rolls Royce engineering plan remains the build discipline. Category 1's visible librarian roadmap is now wired: scope-aware submit/sweep, dedicated `consensus_quorum` draft intake, typed quorum episode/output/adapter-request/result envelopes with file-backed create/load result storage, participant output append/load, independent-first visibility gating, proposed-draft emission, recorded fixture smoke coverage, and `mimir-librarian quorum create|pilot-plan|pilot-status|pilot-run|pilot-review|pilot-summary|append-output|append-status-output|outputs|visible|adapter-request|adapter-plan|adapter-run|adapter-run-round|adapter-run-rounds|synthesize-plan|synthesize-run|accept-synthesis|synthesize|submit-drafts` recorded-artifact commands, draft lifecycle, `run`, `watch`, pre-emit validation, bounded LLM retry, durable commit, supersession-conflict policy, duplicate filtering across all four memory types, configurable same-day valid-at dedup, structured observability, Python prototype retirement, shared workspace write-lock discipline for concurrent MCP+librarian writers, and raw-draft write-time data boundaries with a committed adversarial corpus. The transparent `mimir <agent>` harness now preserves native argv/stdio, prints a concise Mimir preflight banner, discovers bootstrap/config state, derives workspace-log env, writes a structured session capsule with `native_setup` status, emits first-run bootstrap guide/template artifacts, validates setup, surfaces next actions plus governed-log / pending-draft / native-setup / remote-recovery memory status, rehydrates current governed records from an existing canonical log, exposes `MIMIR_SESSION_DRAFTS_DIR` plus `mimir checkpoint` for intentional checkpoint notes, exposes `MIMIR_AGENT_SETUP_DIR` with generated Claude/Codex skill and hook artifacts, provides `mimir status`, `mimir health`, `mimir context`, `mimir memory list|show|explain|revoke`, and `mimir drafts status|list|show|next|skip|quarantine` for operator setup/memory/draft inspection and triage, provides `mimir config init` for safe first-run config creation, provides `mimir hook-context` for hook-safe native context injection with checkpoint-route validation and Claude pre-compaction setup coverage, provides `mimir remote status|push|pull|drill` for explicit Git-backed BC/DR mirroring with checkout/log/draft status, status freshness metadata plus explicit `--refresh`, append-only log checks, relation-aware next actions, conflict remediation recipes, copy-only draft conflict reporting, workspace-lock coordination, divergent-log refusal coverage, a destructive restore drill, opt-in auto-push after capture, Store-open corrupt-tail refusal without destructive truncation, and service-remote status plus dry-run adapter-boundary reporting, provides `mimir setup-agent status|doctor|install|remove` for explicit project/user native setup with dry-run previews, reason-coded partial diagnostics, next-action doctor output, Codex disabled-feature preflight, and non-Mimir skill removal protection, injects Claude context via `--append-system-prompt-file`, injects Codex context via `-c developer_instructions=...`, sweeps configured Claude/Codex native-memory files, stages session-checkpoint and post-session `agent_export` drafts, can run config-driven librarian handoff after capture (`off` / `defer` / `archive_raw` / `process`), can mirror recovery state after capture when `[remote] auto_push_after_capture = true`, blocks process mode when drafts/workspace/LLM prerequisites are missing, and has an integration proof for captured draft -> accepted memory -> next-launch rehydration without relaxing the librarian-governed memory boundary. Copilot is now an official adapter target: `mimir-librarian copilot schema-check|recent|files|checkpoints|search|submit-drafts` opens its local SQLite session store read-only, validates schema before queries, scopes by repository when detectable, fails safely on missing/locked/drifted stores, and submits checkpoint drafts through `copilot_session_store` provenance. The synthesizer adapter contract now converts stored quorum outputs into proposed `QuorumResult` artifacts without letting model synthesis bypass explicit result recording or the librarian draft path; live synthesis status distinguishes process success from result validity, acceptance can use a validated status artifact, saved results require complete one-vote-per-participant validation, `pilot-plan` writes a replayable manifest for multi-round participant execution, synthesis, acceptance, and draft submission and can require a minimum proposed-draft count, `pilot-status` reports pending/complete/failed gates from the recorded artifacts and fails acceptance/submission when required proposed drafts are missing, `pilot-run` executes the manifest through those same gates, reports failed steps with final gate status, exits nonzero on unsuccessful CLI runs, and skips already-complete gates on rerun, `pilot-review` records non-participant certification only after complete status for pass decisions, and `pilot-summary` reports accepted result/review/draft/gate state plus next action. Real local Claude/Codex independent-round pilots completed on 2026-04-25: the first proved execution/synthesis/acceptance/review but no drafts, and the required-draft rerun enforced `--require-proposed-drafts 1`, submitted a `consensus_quorum` draft, and recorded a pass review. Category 9 recovery-benchmark groundwork now has four structured illustrative scenario JSON fixtures plus load-time schema/ID validation for baselines, typed ground truth, and staleness probes; Category 8 now has a `./bench recovery` planner and explicit live path that lists scenarios, renders expected baseline artifacts, materializes non-clobbering result skeletons, prepares per-baseline environment scaffolds, validates environment population input paths, emits non-executing launch contracts, writes all-ready launch contract artifacts, validates materialized launch contracts and prompt files, executes validated contracts only with `--approve-live-execution <scenario-id>`, captures transcripts, fills null mechanical score fields without overwriting operator grades, validates prompt-bearing transcript captures, gates scoring on transcript evidence plus cutoff, staleness, decision-denominator, and integer-type bounds, and summarizes every scenario fixture including missing score files with threshold verdict aggregation. The service-adapter boundary still has dry-run contract output; the next service slice can implement an actual transport against that contract. Recovery pilots may now run only after operator-populated environments validate and the explicit live-execution approval token is supplied."
---

# Mimir — Project Status

> **Snapshot as of 2026-04-28.** Implementation phase: the original 14 architecture specs are `authoritative`; the new [`scope-model.md`](docs/concepts/scope-model.md) and [`consensus-quorum.md`](docs/concepts/consensus-quorum.md) specs are `draft` and record the accepted multi-agent memory-governance plus deliberation mandate. `mimir-mcp` v0.1 read+write surface complete (9 tools, full lease state machine). Test counts are tracked in this file's YAML frontmatter — see `tests.total` / `tests.passing` / `tests.failing` above for live numbers; the body deliberately does not embed counts so a stale narrative cannot drift from the canonical value (the same single-source-of-truth pattern Wick uses). Read-path bench measures p50 ≈ 0.57 µs on a 1 M-memory warm index.

> **Public-readiness note.** Mimir is pre-1.0 active development. The transparent harness, governed draft path, initial bounded `mimir context` surface, initial operator `mimir memory list|show|explain|revoke` controls, and recovery-benchmark live execution path are implemented, but public benchmark claims wait for recorded live wrapped-agent pilots. The 2026-04-28 external memory-product gap scan is tracked in [`docs/planning/2026-04-28-memory-product-gap-scan.md`](docs/planning/2026-04-28-memory-product-gap-scan.md); the strongest near-term product gaps are one-command external client onboarding, richer context sections, relationship/timeline recall UX, and a polished audit/reporting surface.

> **Actions discipline note.** The owner added more GitHub Actions usage and approved re-enabling Actions on 2026-04-27; Actions are currently enabled with `allowed_actions=all`. Treat CI as a confirmation layer, not the first verifier: run the full local gate before every push, batch related changes into one push, and ask before retrying transient infra failures.

## Immediate work order

1. Run live harness/recovery pilots only after the operator-populated baseline environments validate and the explicit scenario approval token is supplied.
2. Complete issue #64 before any visibility change: repository metadata, topics, security-policy check, and explicit owner approval for public visibility.
3. Seed focused public contributor issues, then run the crate-docs and first-run walkthrough audits before the first alpha release.

## Current phase: implementation

Architectural direction is locked in `AGENTS.md` invariants and formally specified in `docs/concepts/`:

- Librarian-mediated single-writer architecture (Roslyn-analog compiler pipeline).
- Agent-native IR, not human-readable; decoder tool for inspection.
- Append-only canonical store with bi-temporal edge invalidation.
- Four-clock temporal model: `valid_at` / `invalid_at` / `committed_at` / `observed_at`.
- Symbol-tracking IR with stable identity, rename propagation, retirement flags.
- Grounding-aware deterministic confidence decay (exponential, class-parameterized, activity-weighted in v1).
- Checkpoint-triggered write batches; each checkpoint is one Episode.
- DAG supersession with bi-temporal edge invalidation (single writer per workspace; no concurrent-write reconciliation).
- Four memory types: semantic, episodic, procedural, inferential. Ephemeral tier alongside for intra-session state.
- Workspace-partitioned stores — per-repo isolation by `hash(git_remote_url)` remains the current implementation boundary.
- Scope-model mandate — raw memories stay isolated at origin scope; cross-project, operator-level, and ecosystem-level reuse happens only through explicit librarian promotion with provenance, trust tier, and revocation.
- Transparent harness mandate — users launch normal agents through `mimir <agent> [agent args...]`; `mimir-harness` now preserves native argv/stdio flow, prints a concise Mimir preflight banner, discovers bootstrap/config state, derives workspace-log env, writes a structured session capsule with native setup status and commands, emits first-run setup artifacts, validates setup, surfaces next actions and memory status including remote recovery metadata, rehydrates current governed records from an existing canonical log with data-only boundary markers, exposes a session-local checkpoint draft inbox plus `mimir checkpoint`, exposes generated Claude/Codex native setup artifacts plus `mimir hook-context`, provides `mimir status`, `mimir health`, `mimir doctor`, `mimir context`, `mimir memory list|show|explain|revoke`, and `mimir drafts status|list|show|next|skip|quarantine` for operator setup/memory/draft inspection and triage, provides `mimir config init` for safe first-run config creation, provides `mimir remote status|push|pull|drill` for explicit Git-backed recovery mirroring with relation-aware remediation, explicit `status --refresh`, canonical-log integrity verification before/after sync, a destructive restore drill that verifies and sanity-queries restored state, opt-in auto-push after capture, and Store-open corrupt-tail refusal without destructive truncation, provides `mimir setup-agent status|doctor|install|remove` for explicit project/user native setup with dry-run previews and reason-coded diagnostics, injects Claude/Codex startup context through their native launch surfaces, sweeps configured Claude/Codex native-memory files, stages checkpoint and post-session `agent_export` drafts, can run config-driven librarian handoff after capture, can mirror recovery state after capture when `[remote] auto_push_after_capture = true`, blocks process mode when prerequisites are missing, and has an integration proof for accepted memory rehydrating into the next launch.
- Copilot adapter mandate — Copilot is an official adapter target. `mimir-librarian copilot schema-check|recent|files|checkpoints|search|submit-drafts` opens the Copilot CLI SQLite session store read-only, validates the expected schema before every query, scopes to the current repository when detectable, fails safely on missing/locked/drifted stores, and submits checkpoint drafts only through the untrusted `copilot_session_store` draft surface with provenance.
- Consensus-quorum mandate — Claude, Codex, and future adapters can deliberate from explicit personas; typed quorum episode/output/adapter-request/result envelopes, file-backed storage, independent-first visibility gating, recorded-artifact CLI commands, native Claude/Codex adapter-plan materialization, bounded adapter-run and multi-round status capture, validated status-to-output append, synthesizer planning/running/acceptance with result validation, and proposed-draft emission now exist, but quorum outputs still remain governed `consensus_quorum` evidence drafts until accepted through the librarian.
- Synchronous in-process write boundary; async queue / status channel / `:read_after` predicate were dropped when `wire-architecture.md` graduated.
- Adapter-mediated agent surfaces — Claude and Codex are the first target surfaces; future agents integrate through the transparent launch harness, draft/retrieval adapters, and optional MCP-compatible tools.
- Librarian implementation language: **Rust**. Compiler-shaped workload fit (algebraic AST enums, ownership, typed errors), deterministic performance without GC, modern agent-memory ecosystem alignment (LanceDB, Turbopuffer, qdrant). Agent-side SDK language(s) TBD — wire format is language-agnostic.
- License: **Apache-2.0**. Explicit patent grant, trademark handling, alignment with the serious-OSS agent/ML ecosystem.

## What's shipped

### Tokenizer bake-off (pre-Phase-1)

Tokens-per-fact measured across 5 candidate write-surface syntaxes on a 50-fact corpus using `tiktoken` `cl100k_base` with `o200k_base` as cross-BPE-family robustness check. Family split finding — JSON (+59%) and Python-call (+41%) meaningfully more expensive than the bareword-value cluster (positional / lisp / sql, all within ~4% of each other); ordinal ranking stable across both tokenizer families. **Decision: Lisp S-expression as v0 write surface**, chosen on architectural grounds (paren structural invariant, self-tagging head, native nested lists, LLM fluency prior) since token differences within the cheap cluster are within noise. Methodology and caveats captured at `research/bake-off/FINDINGS.md` (gitignored).

### Phase 1 — hygiene & destaling (merged, PR #1)

OSS adherence: LICENSE Apache-2.0, CONTRIBUTING, CODE_OF_CONDUCT, SECURITY, CHANGELOG (Keep-a-Changelog 1.1), CITATION.cff, `.github/` templates (PR + 3 issue templates + CODEOWNERS). Docs scaffolding: `docs/concepts/README.md` enumerating expected Phase 3 specs; `docs/attribution.md` with the pending-citation verification log. Rust build-artefact ignores plus design-phase `PRINCIPLES.md` gitignore (graduated in Phase 2).

### Phase 2 — engineering principles (merged, PR #2)

`PRINCIPLES.md` graduated from gitignored draft to tracked. Twelve sections covering testing strategy, error-handling philosophy, type-safety policy, determinism-vs-ML boundary, logging / observability, performance targets, code style & tooling, dependency policy, documentation standard, semantic versioning, deprecation, release process. Framing principle: **optimize for agent consumer, not human-developer ergonomics**.

### Phase 3 — architecture specifications (merged, PR #3 + PR #4 amendments)

Fifteen architecture specs totalling ~5,500 lines in `docs/concepts/`, all marked draft pending Phase 4 primary-source verification. Plus three amendments resolving forward references (Framing enum, `@conflict_reconciliation` inference method, pin / authoritative canonical opcodes).

| Layer | Specs |
|---|---|
| Foundations | Memory Type Taxonomy, Workspace Model, Grounding Model, Symbol Identity Semantics, Temporal Model |
| IR | Write-Surface Grammar, Canonical Form |
| Pipeline | Librarian Pipeline, Write Protocol, Episode Semantics, Read Protocol |
| Decay | Confidence & Decay |
| Boundaries | Wire Architecture, Decoder Tool Contract |

*(A fifteenth spec, `multi-agent-coherence.md`, existed in Phases 3–4 but was deleted on 2026-04-18 when multi-agent coordination was ruled out of scope.)*

`docs/attribution.md` extended to 30+ pending citations across the 15 specs.

### Phase 4 — primary-source verification (merged, PR #6)

All 43 pending citations verified. Five foundational citations (Cahill SSI 2008, Graphiti 2501.13956, Park 2023, Tulving 1972, LangMem) verified individually with full per-source subsections; four surfaced real inaccuracies in Phase-3 drafts (victim-selection claim in multi-agent-coherence, four-clock lineage in temporal-model, rejected-alternative framing in memory-type-taxonomy, episodic-only-not-procedural bounds in Tulving attribution). Remaining 38 batch-verified as canonical references / tool docs / convention references. All 15 specs reach `citation-verified`.

### Milestone 5.1 — Rust foundations (merged)

`mimir_core` crate scaffold with foundational types: `SymbolId`, `ScopedSymbolId`, `SymbolKind`, `WorkspaceId`, `ClockTime`, `Confidence`, `Value`, `MemoryKind` + `MemoryKindTag` + per-variant structs, `SourceKind`. Workspace Cargo.toml, `rust-toolchain.toml` (stable 1.85), `rustfmt.toml`, `deny.toml`, CI workflow (fmt + clippy -D warnings + test + cargo-deny). 39 tests passing (24 unit + 7 property + 8 doctest). `memory-type-taxonomy.md` graduates from `citation-verified` to `authoritative` — first spec to reach full spec status.

### Milestone 5.2 — Lisp write-surface lexer + parser (merged)

`mimir_core::lex` implements the full lexical grammar per `ir-write-surface.md` § 3 — 12 token classes (Symbol, TypedSymbol, Bareword, Timestamp, Integer, Float, String, Boolean, Nil, Keyword, LParen, RParen), typed `LexError` variants (UnterminatedString, InvalidEscape, InvalidNumber, InvalidIdentifier, UnexpectedByte), line/column `Position` tracking. `mimir_core::parse` implements the ten top-level forms (sem / epi / pro / inf / alias / rename / retire / correct / promote / query), order-insensitive keyword args with validation, typed `ParseError` variants, and ISO-8601 timestamp → `ClockTime` conversion via a Howard-Hinnant proleptic-Gregorian calculation (no chrono dep). 68 tests passing (51 unit + 7 property + 10 doctest). `ir-write-surface.md` graduates to `authoritative`.

### Milestone 5.3 — symbol binder (merged)

`mimir_core::bind` implements `symbol-identity-semantics.md`. `SymbolTable` provides monotonic `u64` allocation, canonical-name + alias + retirement tracking, locked `SymbolKind`; `bind()` consumes parser `UnboundForm`s and produces fully-typed `BoundForm` (ids + `Value`s + typed keyword args) with kind inference per position defaults, `@name:Kind` annotation overrides, and registered inference-method validation against the 14-method set from `librarian-pipeline.md` § 5.1. Typed `BindError`: `SymbolKindMismatch`, `SymbolRenameConflict`, `AliasChainLengthExceeded`, `UnknownSymbol`, `BadKind`, `UnregisteredInferenceMethod`, `InvalidKeywordValue`, `UnexpectedList`, `ForeignSymbolForbidden`. 81 tests passing (63 unit + 7 property + 11 doctest). `symbol-identity-semantics.md` graduates to `authoritative` — third spec to reach full spec status.

### Milestone 5.4 — semantic validation (merged)

`mimir_core::semantic` implements `grounding-model.md`. `validate()` consumes `BoundForm`s and produces `ValidatedForm`s with typed fields extracted from keyword bags and the following invariants enforced: source-kind admits memory-kind (rejected with `SourceKindNotAdmitted`); confidence ≤ source's bound (rejected with `ConfidenceExceedsSourceBound`); Episodic `observed_at >= at_time` (rejected with `InvalidClockOrder`); future `valid_at` requires `:projected true` (rejected with `FutureValidity`); Inferential `derived_from` non-empty (rejected with `EmptyDerivedFrom`); `correct` body is Epi (rejected with `CorrectsNonEpisodic`). Source-kind derivation from the 12 reserved grounding-kind names (`@profile`, `@observation`, etc.) with `Observation` as default for arbitrary agent-supplied witness symbols. 93 tests passing (75 unit + 7 property + 11 doctest). `grounding-model.md` graduates to `authoritative` — fourth spec to reach full spec status.

### Milestone 5.5 — canonical bytecode encoder / decoder (merged)

`mimir_core::canonical` implements the on-disk bytecode per `ir-canonical-form.md`. Framing is `[1 byte opcode][LEB128 body length][body]`. Eighteen opcodes covering the four memory record shapes (Sem/Epi/Pro/Inf), four supersession-family edges (Supersedes/Corrects/StaleParent/Reconfirms), the episode-boundary Checkpoint, five symbol-table events (Alloc/Rename/Alias/Retire/Unretire), and four flag events (Pin/Unpin/AuthoritativeSet/AuthoritativeClear). Primitives: LEB128 unsigned varints, ZigZag signed varints for `Value::Integer`, fixed-LE `u64`/`u16` for clocks/confidence/kind-ordinals, value-tag byte + payload for all six `Value` variants. Public API: `CanonicalRecord` sum enum with eight record structs, `encode_record` / `decode_record` / `decode_all`, `Clocks` struct carrying the four clocks plus `NONE_SENTINEL = u64::MAX` for `invalid_at`, `MemoryFlags { projected, stale }` bit-packed. Typed `DecodeError` (7 variants): `Truncated`, `LengthMismatch`, `UnknownOpcode`, `UnknownValueTag`, `InvalidString`, `UnknownSymbolKind`, `BodyUnderflow`, `VarintOverflow`. 116 tests passing (95 unit + 9 property + 12 doctest). `ir-canonical-form.md` graduates to `authoritative` — fifth spec to reach full spec status.

### Milestone 5.6a — librarian pipeline wiring (merged)

`mimir_core::pipeline` ties the four stage modules together: `Pipeline::compile_batch(input, now)` runs parse → bind → semantic → emit, returning `Vec<CanonicalRecord>`. Batch atomicity (spec § 11.3) implemented via clone-on-write: the live `SymbolTable` and memory-ID counter are cloned at batch start and committed only on full-stage success — any mid-batch failure leaves workspace state untouched. Clock injection via the `now: ClockTime` parameter makes the pipeline deterministic for tests (and spec § 11.2 byte-for-byte determinism). Typed `PipelineError` (4 stage-tagged variants: Parse / Bind / Semantic / Emit). Typed `EmitError` (2 variants: Unsupported for forms outside the 5.6a scope — alias/rename/retire/correct/promote/query — and MemoryIdAllocation for the synthesized-name collision path). Memory records emit the four memory kinds (Sem/Epi/Pro/Inf); symbol-event canonical emission is deferred until bind is refactored to journal mutations (spec § 3.4) — that refactor lands together with the write-protocol milestone. 128 tests passing (105 unit + 10 property + 13 doctest), new property test exercises pipeline determinism. **`librarian-pipeline.md` stays `citation-verified`** — it graduates only after 5.6b lands the 14-method inference registry (spec § 1 graduation criterion #4).

### Milestone 5.6b — inference-method registry (merged)

`mimir_core::inference_methods::InferenceMethod` exposes the 14 v1-registered methods (`@direct_lookup`, `@majority_vote`, `@citation_link`, `@analogy_inference`, `@pattern_summarize`, `@architectural_chain`, `@dominance_analysis`, `@entity_count`, `@interval_calc`, `@feedback_consolidation`, `@qualitative_inference`, `@provenance_chain`, `@multi_source_consensus`, `@conflict_reconciliation`). Per-method `parent_count_rule()` + `staleness_rule()` + `compute(parents)` implement the spec's § 5.1 table. All arithmetic is u16 fixed-point with u128 intermediate for products — bit-identical across architectures. Integer Nth root via Newton's method on u128 powers the geomean for `@pattern_summarize`. Parent-count cap of 8 on methods that compute a joint product — bounded-determinism caveat pending a log-table follow-up for unbounded N. `bind` now validates method names via `InferenceMethod::from_symbol_name` (single source of truth; the prior `REGISTERED_INFERENCE_METHODS` string list is retired). 26 new unit tests cover symbol-name round-trip, parent-count rules, per-method formula bounds, range-invariant assertion across all 14 methods, staleness-rule mapping, and integer-Nth-root correctness; 14 per-variant rustdoc doctests satisfy the spec § 5.3 doctest requirement. 169 tests passing (131 unit + 10 property + 28 doctest). Spec amendment in the same PR: `librarian-pipeline.md` § 5.1 now documents the v1 convention that `derived_from` for `@majority_vote` lists only voters-in-favor (so `votes_for == N` and the formula collapses to `min(parents.conf)` — the extra-metadata form is deferred post-v1). **`librarian-pipeline.md` graduates to `authoritative`** — sixth spec to reach full spec status.

### Milestone 5.7a — canonical log + two-phase commit (merged)

`mimir_core::log::CanonicalLog` is the append-only file abstraction per `write-protocol.md` § 3 — `open` / `append` / `sync` (fsync) / `truncate` / `last_checkpoint_end` (forward-scan recovery helper). `mimir_core::store::Store` composes a `CanonicalLog` with a `Pipeline` and exposes `Store::commit_batch(input, now)` — the single public entry point that runs the write-protocol's two-phase commit (spec § 4): compile → append records + `CHECKPOINT` → fsync → ack with typed `EpisodeId`. Any phase failure rolls back both the in-memory pipeline (restored from a clone-on-write snapshot taken before compile) AND the log (truncated to its pre-batch length). On `Store::open`, orphan bytes past the last durable `CHECKPOINT` are truncated — this covers the spec § 7 crash-during-phase-1 row via the recovery-on-next-open path. Typed `StoreError` (`Pipeline` / `Log`). Replay of committed memory records into the fresh `Pipeline` is **explicitly deferred to 5.7b** — it depends on `SymbolAlloc`/`Rename`/`Alias` canonical records which in turn depend on refactoring `bind` to journal mutations (spec § 3.4). 185 tests passing (147 unit + 10 property + 28 doctest); 16 new unit tests (7 for `CanonicalLog` — open/append/sync/truncate/scan — plus 8 for `Store` — commit, multi-commit, pipeline-error rollback, distinct episode IDs, orphan truncation on reopen, commit-path rollback via synthesized `__ep_{n}` collision). **`write-protocol.md` stays `citation-verified`** — it graduates after 5.7b lands the bind-mutation-journal refactor and the full crash-injection matrix.

### Milestone 5.7b — bind mutation journal + symbol replay (merged)

`bind::bind` now returns `(Vec<BoundForm>, Vec<SymbolMutation>)` per `librarian-pipeline.md` § 3.4 — every symbol-table allocation / rename / alias / retire is recorded in a journal alongside the bound AST. The pipeline's emit stage serializes the journal into `SymbolAlloc` / `SymbolRename` / `SymbolAlias` / `SymbolRetire` canonical records (opcodes 0x30 / 0x31 / 0x32 / 0x33) that precede the batch's memory records, so log replay sees allocations before the memory records that reference them. `Alias` / `Rename` / `Retire` validated forms no longer produce memory-level records (they're handled entirely via the journal) and no longer return `EmitError::Unsupported`. `SymbolTable::replay_{allocate,alias,rename,retire}` reconstitute a table from a log; `Store::open` decodes the log, replays the SYMBOL_* events into a fresh `Pipeline`, and advances the monotonic memory and episode counters past the highest-numbered `__mem_{n}` / `__ep_{n}` symbol seen — so a workspace's in-memory state fully reconstructs across process restarts. 188 tests passing (150 unit + 10 property + 28 doctest). Three new unit tests exercise the close/reopen round-trip (`reopen_restores_symbol_table_from_log` asserts the `alice` symbol's exact SymbolId persists across the boundary, `reopen_advances_memory_and_episode_counters` verifies counter restoration, `reopen_replays_rename_and_retire` covers non-allocation mutations). **`write-protocol.md` stays `citation-verified`** — graduation requires 5.7c's crash-injection matrix (spec § 7).

### Milestone 5.7c — crash-injection matrix (merged)

`LogBackend` trait abstracts the filesystem primitives (`append` / `sync` / `truncate` / `read_all` / `last_checkpoint_end`); `CanonicalLog` is the default production impl, `FaultyLog` is the test-only in-memory impl with armable failure hooks. `Store` is now generic over `Store<L: LogBackend = CanonicalLog>` — production code still uses `Store::open(path)`; tests use `Store::from_backend(FaultyLog::new())` to inject faults. Seven new unit tests exercise the `write-protocol.md` § 7 failure-mode matrix: orphan-memory-record-without-checkpoint truncated on reopen (row 3); append-failure triggers full rollback of log + pipeline + counter (row 6, `StorageFull` injected); sync-failure triggers rollback including truncation of the appended bytes (row 7); rollback preserves earlier committed bytes; orphan truncation is idempotent; symbol-table replay reproduces exact pre-crash state (spec § 1 criterion #4, SymbolTable byte-equal across close/reopen); CHECKPOINT is atomic commit boundary (truncating mid-log discards the later batch but keeps the earlier one). One new property test in `tests/properties.rs` covers § 10.2 orphan-truncation idempotence across random orphan suffixes. Also fixed: memory-id `__mem_{n}` allocations now emit `SymbolAlloc` canonical records in the same batch, so replay reconstructs the full table (previously only bind-journal-derived symbols were serialized). 196 tests passing (157 unit + 11 property + 28 doctest). **`write-protocol.md` graduates to `authoritative`** — seventh of fifteen specs.

### Milestone 5.8 — confidence-decay core (merged)

`mimir_core::decay` implements the deterministic exponential-decay formula per `confidence-decay.md` § 5.1 using integer fixed-point arithmetic at the u16 confidence resolution. A 256-entry `DECAY_TABLE` of `round(2^(-i/256) × 65535)` values is baked into source (hand-generated with Python's `math.pow`, then hard-coded) so every build is byte-identical — satisfying spec § 13 invariant 2 (bit-identity across architectures) without relying on libm `powf`. The integer exponent part is a right-shift; the fractional part is a table lookup. Public API: `decay_factor_u16(elapsed_ms, half_life_ms) -> u16`, `effective_confidence(stored, elapsed_ms, memory_kind, source_kind, flags, config) -> Confidence`. `DecayConfig` carries the v1 default half-life table (spec § 5.2) with all fields `pub` for runtime user override; `NO_DECAY = 0` encodes the § 5.3 infinity case (`@librarian_assignment`, Procedural time-decay). `DecayFlags { pinned, authoritative }` short-circuits the formula to `stored` per § 13 invariant 3. 15 new unit tests cover table monotonicity, boundary conditions (zero elapsed, infinite half-life, 16+ half-lives saturates to zero, pinned/authoritative skip), spec-table defaults, and runtime override behavior. 4 new property tests cover the three spec § 13 invariants verifiable at this layer: determinism, monotonicity in elapsed, pinned/authoritative skip, and `NO_DECAY` preservation. 217 tests passing (172 unit + 15 property + 30 doctest). **`confidence-decay.md` stays `citation-verified`** — graduation waits on 5.8b's decay TOML loader to satisfy spec § 1 criterion #4 (file-backed user overrides).

Procedural activity weighting (§ 6) and Inferential parent-tracking composition (§ 9 — callers compose with `InferenceMethod::compute`) are explicitly deferred to follow-up PRs. (The cross-workspace-inheritance-acceleration line item was cut on 2026-04-18 when cross-workspace imports left scope.)

### Milestone 5.8b — decay TOML loader + graduation (merged)

Added `toml = "0.8"` with `default-features = false, features = ["parse"]` as a dep (license-clean — MIT/Apache-2.0 throughout the resolution chain, surfaces `serde_core`/`serde_spanned` transitively which cargo-deny allows). `DecayConfig::from_toml(&str)` and `DecayConfig::apply_toml(&mut self, &str)` parse the spec § 5.2 TOML shape (`[decay.semantic]`, `[decay.episodic]`, `[decay.procedural]` sections with days-valued integer keys; `0` encodes the § 5.3 `NO_DECAY` infinity sentinel; unlisted keys fall back to librarian defaults; unknown keys silently ignored for forward-compat). Typed `DecayConfigError` (`Parse` / `ExpectedTable` / `ExpectedNonNegInteger` / `InvalidDays`). 10 new unit tests exercise every error path and the integration-test-shaped `toml_reload_changes_effective_confidence_without_restart` that proves the spec § 1 graduation criterion #4 (runtime override without restart: same `(stored, elapsed)` produces a strictly lower effective confidence after the reload). 227 tests passing (182 unit + 15 property + 30 doctest). **`confidence-decay.md` graduates to `authoritative`** — eighth of fifteen specs.

### Milestone 5.9 — workspace-model graduation (merged)

`mimir_core::workspace` extended with `WorkspaceError`, `WorkspaceId::detect_from_path` (the § 3.3 ancestor walk that finds `.git/` and reads its `config`), and `parse_git_config_origin_url` (in-process parser tolerant of quoted/unquoted section headers, tabs, and `#`/`;` comments — no `git` subprocess dep). `Store::open_in_workspace(data_root, workspace_id)` creates the partitioned `data_root/<32-byte-hex>/canonical.log` directory layout from § 4.2, keeping the partition structural rather than policy-enforced. Two property tests cover graduation criterion #3: `workspace_id_determinism` (normalised URLs hash identically regardless of case or `.git` suffix) and `workspace_partitioning_is_structural` (writes to workspace A leave workspace B's reopened Store empty). 13 new tests total (8 unit + 2 property + 3 integration-shaped for the detect / partition flow). 240 tests passing (193 unit + 17 property + 30 doctest). **`workspace-model.md` graduates to `authoritative`** — ninth of fifteen specs.

### Milestone 5.10 — decoder-tool-contract graduation (merged)

New `crates/mimir-cli` binary crate (depends on `mimir_core` only — no `mimird`, per spec § 3) with four v1 subcommands: `log` (stream summary), `decode` (emit re-parseable Lisp for memory records), `symbols` (print reconstructed table), `verify` (integrity + corruption report). `mimir_cli::LispRenderer` renders SemRecord / EpiRecord / ProRecord / InfRecord as write-surface Lisp, resolving `SymbolId` → canonical name via a `SymbolTable` reconstructed from `SYMBOL_*` log events. `mimir_cli::iso8601_from_millis` is the deterministic inverse of the parser's ISO-8601 loader (Howard-Hinnant proleptic-Gregorian, integer-only — no chrono). `mimir_cli::verify` walks the log, replays symbol events into a fresh table, and returns a structured `VerifyReport` (records_decoded / checkpoints / memory_records / symbol_events / trailing_bytes / dangling_symbols) — CLI exit code is `0` for clean, `1` for corruption signals, `2` for argument errors. Seven integration tests cover § 10 invariants 1 / 2 / 3 / 4 / 7; two test the corruption-detection contract (orphan tail + corrupted opcode). `SymbolTable::iter_entries` added to `mimir_core::bind` for the CLI's `symbols` subcommand. 254 tests passing (193 core unit + 17 core property + 30 core doctest + 7 mimir-cli unit + 7 mimir-cli integration). **`decoder-tool-contract.md` graduates to `authoritative`** — tenth of fifteen specs.

### Milestones 6.1 → 9.7 — read protocol, episode semantics, observability, wire-architecture (merged in PRs #37–#54)

Implementation rounded out across the back half of the spec corpus. Per-milestone narration omitted here to keep this section focused on the next 30 days; the per-milestone story (each PR's scope, test-count delta, spec graduation) is in [`CHANGELOG.md`](CHANGELOG.md) `[Unreleased]`. Headline shape:

- **6.x (read protocol):** `Pipeline::execute_query` hot-path reader, `:kind`/`:s`/`:p`/`:as_of`/`:as_committed`/`:limit`/`:include_retired`/`:include_projected`/`:confidence_threshold`/`:explain_filtered`/`:show_framing`/`:debug_mode` predicates, `STALE_SYMBOL`/`LOW_CONFIDENCE`/`PROJECTED_PRESENT`/`TRUNCATED`/`EXPLAIN_FILTERED_ACTIVE` flag bitset, `Framing` enum, `semantic_by_sp_history` + `procedural_by_rule_history` current-state indices, `read-protocol.md` graduates.
- **7.x (read property tests + decay-effective LOW_CONFIDENCE):** snapshot isolation, STALE_SYMBOL flag content equivalence, as-of + retroactive supersession, `LOW_CONFIDENCE` driven by `decay::effective_confidence`.
- **8.x (episode semantics):** `(episode :start :label "x" :parent_episode @E :retracts (@E1))` and `(episode :close)` write-surface forms, `EpisodeMeta` durable record (opcode `0x21`), `episode_committed_at` + `episode_parent` + `MAX_EPISODE_CHAIN_DEPTH = 1024`, Episode-scoped read predicates `:in_episode` / `:after_episode` / `:before_episode` / `:episode_chain`, `episode-semantics.md` graduates.
- **9.x (observability + audit follow-ups + wire-architecture):** `tracing` spans + events per [`docs/observability.md`](docs/observability.md) with privacy invariants asserted in tests; pin/authoritative write surface; Inferential staling write-path (reverse-parent index + StaleParent edges); HalfLife newtype refactor; `MemoryFlags` split into `SemFlags` / `InfFlags` (wire-format break, decoder strict-rejects pre-break logs); fuzz harness (3 targets — `fuzz_lex`, `fuzz_parse`, `fuzz_decoder`); `wire-architecture.md` graduates with the **in-process-only** scope call (daemon / async queue / `:read_after` predicate dropped).
- **Phase 0 → 1.5 (audit-to-fix sweep + foundation hardening):** README/AGENTS/CHANGELOG honesty sweep; SECURITY.md threat model rewrite; 3-OS CI matrix + Dependabot + cargo-deny + SHA pinning; 5 doc/code drift gates; decoder DoS fixes (canonical log magic header, decoder OOM caps, parser stack-depth bound); release pipeline scaffold + RELEASING.md.
- **Phase 2.1 → 2.4 (`mimir-mcp` end-to-end):** library + binary serving 9 tools over stdio MCP; workspace lease state machine (open / write / close-episode / release with constant-time token validation, lazy expiry, configurable TTL); read tools wrap `Pipeline::execute_query` / `LispRenderer` / `verify` / `iter_episodes`; tool-catalog drift gate enforces tool surface ↔ README sync and prevents unsupported client-surface drift.

Cumulative outcome: **all 14 architecture specs are `authoritative`**; the v1.1 fresh assessment's P0 ("agent unreachable") is closed; live test count tracked in this file's frontmatter (`tests.total`).

### Audit-to-fix sweep (Phase 0 of the prime-time roadmap, this merge)

Doc-honesty / per-crate-metadata / hygiene sweep closing the morning quick-wins from the v1.1 fresh assessment ([`analysis/reports/Mimir-analysis.md`](../analysis/reports/Mimir-analysis.md) in the parent workspace). 13 file edits in one PR. Roadmap from this point lives at [`docs/planning/2026-04-19-roadmap-to-prime-time.md`](docs/planning/2026-04-19-roadmap-to-prime-time.md).

## What's in flight

Implementation phase. **All 14 architecture specs are `authoritative`**. `wire-architecture.md` graduated on 2026-04-19 with the deployment-mode scope call resolved: in-process only. Daemon / async queue / status channel / `:read_after` all dropped as unjustified under the Claude-single-writer target. Phase 7 (read protocol) graduated in PRs #37–#43. Episode-semantics graduated in PRs #44 / #45 / 8.3. Step 5 deferred issues (#33, #31, #29, #32, #30) all closed. Observability (#30) locked the tracing schema in `docs/observability.md`.

Client-integration research companion (gitignored): `research/CLIENT_INTEGRATION_NOTES.md` captures post-Phase-3 client-library scope — Claude Code hooks / skills / MCP / subagents / web-tree features. Not in the immediate milestone path; belongs to the client-library design phase once the librarian core stabilises.

## Next milestones

`PLAN.md`'s Step 1–6 implementation roadmap is **complete**. The forward roadmap has shifted from "feature completeness" to "publicly listed v1.0" — see [`docs/planning/2026-04-19-roadmap-to-prime-time.md`](docs/planning/2026-04-19-roadmap-to-prime-time.md) for the phased plan (6 phases over ~6–9 weeks of focused work).

Headline (post-cutover re-assessment, 2026-04-20 T1430): **C+ (uncapped, weighted 3.70)** — down from B (4.00) at T0237 on the same day. The drop is **transient post-cutover residue** the T0237 audit could not have known about: a `release.yml` package-id bug (`-p mimir_core` vs actual `mimir-core` — first crates.io publish would fail; the session's **only** P0), stale `engram_core` paths in CODEOWNERS, `EngramServer` identifier still in the public API, and 8+ `engram` / `EGRM` string remnants in user-facing surfaces. Substantive engineering (lease security, test coverage, drift gates, release pipeline structure, threat model) is all in place. The post-cutover cleanup PR closes 1 P0 + 4 P1 + 8 P2 + most P3, returning the grade to B+/A-. See [`../analysis/reports/Mimir-analysis.md`](../analysis/reports/Mimir-analysis.md) (parent workspace) for the v1.1 post-cutover synthesis.

Post-v1 candidates (Phase 6 of the roadmap): LLM-fluency re-measurement on the released wire surface, DAG / snapshot-divergence corruption rows for `verify`, out-of-process daemon (if ever motivated), Inferential read-time staling overlay, post-hoc Episode label update, Episode timeout auto-close, write-path criterion bench, continuous fuzzing in CI.

## Blockers

None.

## References

- [`AGENTS.md`](AGENTS.md) — operating manual, architectural invariants, engagement protocol, anti-patterns.
- [`PRINCIPLES.md`](PRINCIPLES.md) — engineering principles and tooling policy (Phase 2).
- [`docs/concepts/`](docs/concepts/) — 14 architecture specs, all `authoritative`.
- [`docs/planning/2026-04-24-transparent-agent-harness.md`](docs/planning/2026-04-24-transparent-agent-harness.md) — accepted product-direction record for `mimir <agent> [agent args...]`.
- [`docs/observability.md`](docs/observability.md) — tracing span + event schema contract.
- [`docs/attribution.md`](docs/attribution.md) — primary-source verification log (Phase 4 work lands here).
- `research/bake-off/FINDINGS.md` — tokenizer bake-off results (gitignored).
- `research/GAP_ANALYSIS.md` — phase-1-to-4 gap analysis (gitignored).
- `research/CLIENT_INTEGRATION_NOTES.md` — post-Phase-3 client-library research scope (gitignored).
